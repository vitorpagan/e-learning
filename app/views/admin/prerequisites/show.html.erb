<div class="row">
  <div class="col-md-offset-2 col-md-8">
    <h1><%= @prerequisite.name %></h1>
    <div class="form-group">
      <label>Data de Criação:</label> <%= l(@prerequisite.created_at, format: :full) %>
    </div>
    <div class="text-center">
      <%= link_to 'Voltar', admin_prerequisites_path, class: 'btn btn-default' %> <%= link_to 'Editar', edit_admin_prerequisite_path(@prerequisite), class: 'btn btn-primary' %>
    </div>
  </div>
</div>