<p>
  <strong><%= Category.human_attribute_name(:name)%>:</strong>
  <%= @category.name %>
</p>
<p>
  <strong>Data de criação:</strong>
  <%= l(@category.created_at, format: :full) %>
</p>


<div class="text-center">
  <%= link_to 'Voltar', admin_categories_path, class: 'btn btn-default' %> <%= link_to 'Editar', edit_admin_category_path(@category), class: 'btn btn-primary' %>
</div>